#ingress.yml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-controller
  namespace: dev
spec:
  ingressClassName: haproxy  # Use this field instead of the deprecated annotation
  rules:
  - http:
      paths:
      - path: /web-app
        pathType: Prefix
        backend:
          service:
            name: nginx-service
            port:
              number: 80
    # - path: /web-app
    #   pathType: Prefix
    #   backend:
    #     service:
    #       name: lb-service-2
    #       port:
    #       number: 80
---