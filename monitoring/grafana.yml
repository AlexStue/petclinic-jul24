apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus.monitoring.svc.cluster.local:9090  # Use Prometheus Service URL
    isDefault: true
    jsonData:
      timeInterval: "5s"